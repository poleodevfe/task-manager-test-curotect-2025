# Environment variables for the Task Manager Backend

# This is the connection string for the PostgreSQL database running in Docker.
# The 'db' hostname refers to the postgres service name in docker-compose.yml.
DATABASE_URL="postgresql://myuser:mypassword@db:5432/taskdb?schema=public"

# The port the backend server will listen on.
PORT=3001
